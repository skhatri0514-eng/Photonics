# Photonics

## Python environment

Use the local `pic` conda environment for FDTD work with Meep:

```bash
source .miniforge3/bin/activate pic
```

Apple's system `python3` will not see Meep. Run simulations from the `pic`
environment instead.
